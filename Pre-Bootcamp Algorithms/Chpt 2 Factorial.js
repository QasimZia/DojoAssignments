function factorial(num) {
    //var to store product
    var product = 0;

    //for loop iterating through integers up to and inclusive of num
    for(var x = 1; x <= num; x++) {

        //calculate factorial
        product = product * x;

    }
}