function makeItBig (array) {

    //for loop iterating through array
    for (var x = 0; x < array.length; x=++) {

        //if statement comparing whether value at specified index is positive or negative
        if (array[x] > 0) {
            
            //if true, change value at x index to "big"
            array[x] = "big";

        }
    }

}