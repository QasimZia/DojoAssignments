function scaleArray(arr, num) {

    //for loop to cycle through array
    for(var x = 0; x < array.length; x++) {

        //cycle through each value in array and multiply by given num, assign new value
        array[x] = (array[x] * num);

    }

    return arr;
}