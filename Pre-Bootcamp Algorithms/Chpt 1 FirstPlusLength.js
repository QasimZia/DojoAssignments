function firstPlusLength() {
    //declare array and sum zero index + length
    var array = [1,2,3,4];
    var sum = array[0] + array.length;
    //return sum
    return sum;

}

//output result
console.log(firstPlusLength());

