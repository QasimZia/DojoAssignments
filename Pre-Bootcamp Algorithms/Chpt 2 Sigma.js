function sigma(num) {
    //var to record sum
    var sum = 0;

    //for loop to cycle through numbers up to and including var num
    for(var x = 0; x <= num; x++) {

        sum += x;

    }

    return sum;
}