function arrayOdd() {
    //create array to store values
    var arrayOdd = [];

    //for loop to cycle through array
    for(var x = 1; x <= 255; x = x + 2) {

        //add values to array
        arrayOdd.push(x);
    }
}