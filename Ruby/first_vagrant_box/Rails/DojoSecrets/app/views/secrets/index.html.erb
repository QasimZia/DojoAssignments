<h1>Secrets, <a href='/users/<%= current_user.id %>'>Home</a></h1>
<% @secrets.each do |secret| %>
    <p>
    <%= secret.content %>
    <% if secret.user_id == current_user.id %>
        <a href='/secrets/<%= secret.id %>'/destroy>Delete</a>
    <% end %>
    </p>
<% end %>
